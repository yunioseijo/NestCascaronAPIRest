
STAGE=dev

DB_PASSWORD=MySecr3tPassWord@as2
DB_NAME=TesloDB
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres

PORT=3000
HOST_API=http://localhost:3000/api
APP_WEB_URL=http://localhost:4200

JWT_SECRET=Est3EsMISE3Dsecreto32s

# Seed configuration
# Protects the /seed endpoint; change in prod
SEED_SECRET=dev-seed
# Initial admin user
ADMIN_EMAIL=admin@ahucha.local
ADMIN_PASSWORD=Admin123!
ADMIN_NAME=Super Admin
SEED_TEST_PASSWORD=Test123!
SEED_BULK_USERS=20

# CORS (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:4200

# SMTP / Mailer (for Nodemailer)
# MAIL_PROVIDER controls the transport: ethereal | gmail | mailtrap | smtp
# If not set or incomplete, Ethereal preview links will be used in dev logs
MAIL_PROVIDER=ethereal
MAIL_FROM="Ahucha <no-reply@ahucha.local>"

# Generic SMTP creds (used by gmail/mailtrap/smtp as noted)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Quick presets (uncomment and fill)
# Gmail (requires 2FA and App Password):
# MAIL_PROVIDER=gmail
# SMTP_USER=tu_cuenta@gmail.com
# SMTP_PASS=tu_app_password

# Mailtrap (recommended in dev):
# MAIL_PROVIDER=mailtrap
# SMTP_USER=YOUR_MAILTRAP_USER
# SMTP_PASS=YOUR_MAILTRAP_PASS
